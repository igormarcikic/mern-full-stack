import React from 'react'
import {
    Container
} from '@material-ui/core';

const Register = () => {
    return (
        <Container>
            <form>
                <input type="text"/>
            </form>
        </Container>
    )
};

export default Register;